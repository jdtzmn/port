name: Setup Port
description: Install pinned Bun and Node runtimes, restore dependencies, install dependencies, build Port, and verify its Node CLI.

inputs:
  configure-git-identity:
    description: Configure the Git identity required by repository fixture tests.
    required: false
    default: 'false'

runs:
  using: composite
  steps:
    - name: Configure git identity
      if: inputs.configure-git-identity == 'true'
      shell: bash
      run: |
        git config --global user.email "ci@test.local"
        git config --global user.name "CI Runner"

    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: 22

    - name: Setup Bun
      uses: oven-sh/setup-bun@v2
      with:
        bun-version: 1.3.14

    - name: Restore Bun download cache
      uses: actions/cache@v4
      with:
        path: ~/.bun/install/cache
        key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
        restore-keys: |
          ${{ runner.os }}-bun-

    - name: Install dependencies
      shell: bash
      run: bun install --frozen-lockfile

    - name: Build
      shell: bash
      run: bun run build

    - name: Verify Node runtime
      shell: bash
      run: bun run test:node-runtime
